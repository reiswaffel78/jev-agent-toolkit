{
  "_comment": "Claude Code project-scoped .mcp.json. Launches the published npm package with npx. Claude Code expands ${VAR}, so the key stays in your environment. Remove this _comment key before use.",
  "mcpServers": {
    "jev": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "jev-agent-toolkit-mcp"],
      "env": {
        "TYPESAFE_API_KEY": "${TYPESAFE_API_KEY}"
      }
    }
  }
}
