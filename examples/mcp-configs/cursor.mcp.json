{
  "_comment": "Cursor .cursor/mcp.json. NOTE the ${env:VAR} syntax — it differs from Claude Code's ${VAR}. Never commit a real key.",
  "mcpServers": {
    "jev": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "jev-agent-toolkit-mcp"],
      "env": {
        "TYPESAFE_API_KEY": "${env:TYPESAFE_API_KEY}"
      }
    }
  }
}
