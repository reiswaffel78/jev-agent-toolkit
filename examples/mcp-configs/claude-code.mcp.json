{
  "_comment": "Claude Code project-scoped .mcp.json. Copy to the repo root as .mcp.json. Claude Code expands ${VAR} — keep the key in your environment, never in this file.",
  "mcpServers": {
    "jev": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "jev-agent-toolkit-mcp"],
      "env": {
        "TYPESAFE_API_KEY": "${TYPESAFE_API_KEY}"
      }
    }
  }
}
