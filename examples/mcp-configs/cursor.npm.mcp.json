{
  "_comment": "Cursor .cursor/mcp.json. Launches the published npm package with npx. NOTE the ${env:VAR} syntax, which differs from Claude Code's ${VAR}. Remove this _comment key before use.",
  "mcpServers": {
    "jev": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "jev-agent-toolkit-mcp"],
      "env": {
        "TYPESAFE_API_KEY": "${env:TYPESAFE_API_KEY}"
      }
    }
  }
}
